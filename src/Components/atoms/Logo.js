import React from 'react';
import styled from 'styled-components';

const StyledWrapper = styled.div`
  width: 100%;
  max-height: 100px;
  display: flex;
  flex-grow: 1;
`;

const Logo = () => <StyledWrapper />;

export default Logo;
